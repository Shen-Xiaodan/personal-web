# 个人网站

一个使用 React 和 Vite 构建的个人网站项目。

## 命令

- `npm install` 安装依赖
- `npm run dev` 启动开发服务器
- `npm run build` 构建生产版本
- `npm run preview` 预览生产构建

## 目录

```
public/
src/
  components/
  styles/
  App.jsx
  index.css
  main.jsx
index.html
package.json
vite.config.js
```
